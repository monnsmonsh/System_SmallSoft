
@model IEnumerable<IdentitySample.Models.ApplicationUser>

@{
    ViewBag.Title = "Usuarios";
    <!---->ViewBag.UsuariosA = "active";
}

<!-- modal large -->
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="largeModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="largeModalLabel"></h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div id="modal-content">
                    <!---->
                    Cargando...
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Cancel</button>
            </div>
        </div>
    </div>
</div>
<!-- end modal large -->

<!--
<br />
<h2 class="display-4 font-weight-normal">Usuarios</h2>
-->

<div class="jumbotron" style="background-image: url(/Content/img/about-service.png); background-size: 100%;">
    <div class="container">
        <h1 class="display-3" _msthash="669669" _msttexthash="169715" style="color:dimgrey;">Usuarios</h1>



    </div>
</div>
@*<a class="btn btn-primary btn-lg" href="~/UsersAdmin/Create" role="button">Nuevo Usuario</a>
    <p>
            @Html.ActionLink("Create New", "Create")
        </p>*@

<div class="btn-group btn-group-toggle" data-toggle="buttons">

    <label class="btn btn-secondary btn-lg">
        <a href="~/UsersAdmin/Create" role="button" style="color: #fff; "> Nuevo Usuario</a>
    </label>
    <label class="btn btn-secondary btn-lg">
        <a href="~/RolesAdmin/Create" role="button" style="color: #fff; "> Nuevo Rol</a>
    </label>
</div>

<div class="row">

</div>
<br />
<br />
<table id="tblListUser" class="table table-striped table-bordered">
    <thead class="thead-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>

            </th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td>
                    @*<a href="~/UsersAdmin/Edit/@item.Id" class="btn btn-danger btn-sm" style="background-color: #007bba; border-color: #007bba;">
                        <i class="fa fa-pencil-square-o"></i>
                    </a>*@
                    <button href="#myModal" class="btnEditUser btn btn-danger btn-sm" data-toggle="modal" data-id="@item.Id" style="background-color: #007bba; border-color: #007bba;">
                        <i class="fa fa-pencil-square-o"></i>
                    </button>
                    <button href="#myModal" class="btnDetailsUser btn btn-danger btn-sm" data-toggle="modal" data-id="@item.Id" style="background-color: #28a745; border-color: #28a745;">
                        <i class="fa fa-list-alt"></i>
                    </button>
                    <button href="#myModal" class="btnDeleteUser btn btn-danger btn-sm" data-toggle="modal" data-id="@item.Id">
                        <i class="fa fa-trash"></i>
                    </button>

                    @*@Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })*@
                </td>
            </tr>
        }
    </tbody>

</table>
